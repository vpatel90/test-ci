language: node_js
services:
  - docker
node_js:
  - "stable"

before_install:
  - docker build -t service .
  - docker ps -a

cache:
  directories:
    - "node_modules"

scrips:
  - docker run service /bin/sh -c "npm test"